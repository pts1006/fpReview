function search(count) {
	for (let i = 0; i < count; i++) {
		console.log($("[data-row-key=" + i + "][data-column-name='lyear']").find("div").text());
	}
	const lyear = $("#year").val();
	const term = $("#term").val();
	$.ajax({
		url: "scoreSearch",
		type: "post",
		data: {

		},
		success: function(result) {
			console.log(result);
		},
		error: function(err) {
			console.log(err);
		}
	});
}